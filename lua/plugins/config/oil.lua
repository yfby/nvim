require("oil").setup({
  default_file_explorer = true,
  columns = {
    "icon",
  },
  skip_trailing_separator_on_line_end = true,
  use_default_keymaps = true,
  view_options = {
    show_hidden = true,
  },
})
